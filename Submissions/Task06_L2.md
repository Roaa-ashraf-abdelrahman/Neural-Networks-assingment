# Task 6 - L2
- L2 reduced weight magnitude by penalizing large weights during training
- smaller weights encourages simpler models so it improves generalization
- it aims to decrease validatin loss for long period then makes it stable to prevent upward trend associated with overfitting
